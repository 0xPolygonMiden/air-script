def SelectorsAir

trace_columns:
    main: [s[3], clk]
    
public_inputs:
    stack_inputs: [16]

boundary_constraints:
    enf clk.first = 0

integrity_constraints:
    enf clk' = 0 when s[0] & !s[1]
    match enf:
        clk' = clk when s[0] & s[1] & s[2]
        clk' = 1 when !s[1] & !s[2]