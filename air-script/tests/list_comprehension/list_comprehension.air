def TraceColGroupAir

trace_columns:
    main: [clk, fmp[2], ctx]
    aux: [a, b, c[3]]

public_inputs:
    stack_inputs: [16]

integrity_constraints:
    let x = [fmp for fmp in fmp]
    enf clk = x[0]
  
boundary_constraints:
    enf c[2].first = 0